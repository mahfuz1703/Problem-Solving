#include <bits/stdc++.h>
using namespace std;
#define vs void solve()
#define ll long long
#define vi vector<int>
#define loop(x,n) for (int i = x; i < n; i++)

vs{
    string s; cin >> s;
    s[0] = toupper(s[0]);
    cout << s << "\n";
}
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    solve();
    return 0;
}
